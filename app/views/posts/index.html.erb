<% @posts.each do |post| %>
  <div class="post_wrapper">
    <h3 class="title"><%= link_to(post[:title], controller: "posts", action: "show", id: post.id) %></h2>
    <p class="date"><%=  post[:create].to_formatted_s(:long_ordinal) %></p>
  </div>
<% end %>